const tasks=[{title:'Draft your project walkthrough',meta:'45 min · Best match for your current signal',pill:'BEST NEXT',green:true,done:false},{title:'Reply to the research teammate',meta:'15 min · Quick win before deep work',pill:'QUICK WIN',done:false},{title:'Review tomorrow’s presentation notes',meta:'25 min · Due today at 18:00',pill:'DUE TODAY',done:false}];
const list=document.getElementById('taskList'); function render(){list.innerHTML=tasks.map((t,i)=>`<div class="task"><button class="check ${t.done?'done':''}" data-i="${i}" aria-label="Complete task"></button><div class="task-body"><div class="task-title ${t.done?'done':''}">${t.title}</div><div class="task-meta">${t.meta}</div></div><span class="pill ${t.green?'green':''}">${t.pill}</span></div>`).join(''); document.querySelectorAll('.check').forEach(b=>b.onclick=()=>{tasks[+b.dataset.i].done=!tasks[+b.dataset.i].done;render()})}render();document.getElementById('startBtn').onclick=()=>{document.getElementById('startBtn').innerHTML='Session started <span>✓</span>';document.getElementById('startBtn').style.background='#d9f5eb';document.getElementById('startBtn').style.color='#168d69'};document.getElementById('addBtn').onclick=()=>{const title=prompt('What should PulsePilot remember?');if(title){tasks.push({title,meta:'Added just now · Neural memory ready',pill:'NEW',green:true,done:false});render()}};
